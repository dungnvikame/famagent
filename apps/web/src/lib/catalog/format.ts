export const vnd = (amount: number): string => `${Math.round(amount).toLocaleString("vi-VN")}đ`;

/** Affiliate disclosure (spec §49): shown near recommendations, on compare, product pages and in the footer. */
export const AFFILIATE_DISCLOSURE = "FamAgent có thể nhận hoa hồng khi bạn mua qua một số liên kết. Điều này không ảnh hưởng đến thứ tự đề xuất.";
